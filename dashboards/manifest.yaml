manifestVersion: 1.0
projects:
  - name: executive-service-slo-dashboard
    path: executive-service-slo-dashboard

environmentGroups:
  - name: default
    environments:
      - name: DT_ENV_ID
        url:
          value: DT_ENV_URL
        auth:
          token:
            name: API_TOKEN
          oAuth:
            clientId:
              name: CLIENT_ID
            clientSecret:
              name: CLIENT_SECRET