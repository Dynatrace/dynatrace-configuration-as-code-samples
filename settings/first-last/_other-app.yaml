configs:

- id: other-app
  type: application-web
  config:
    name: "Other application"
    template: "application.json"

- id: other-app-rule
  type:
    settings:
      schema: builtin:rum.web.app-detection
      scope: environment
      insertAfter: front
  config:
    template: "rule.json"
    parameters:
      applicationId:
        type: reference
        configType: application-web
        configId: other-app
        property: id
      pattern: "other.app.dynatrace"
