manifestVersion: 1.0

projects:
  - name: service-level-objectives

environmentGroups:
  - name: default
    environments:
      - name: kci
        url:
          type: environment
          value: ENVIRONMENT_URL
        auth:
          oAuth:
            clientId:
              name: OAUTH_CLIENT_ID
            clientSecret:
              name: OAUTH_CLIENT_SECRET